name: First Workflow

on:
  workflow_dispatch:
  schedule:
    - cron: '*/5 * * * *'

jobs:
  first-job:
    runs-on: ubuntu-latest

    steps:
      - name: Print Greetings
        run: |
          echo "Hello Everyone"
          echo "How are you?"

      - name: Print Bye
        run: |
          echo "Goodbye, see you later"
          echo "Alligator"
